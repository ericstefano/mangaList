const Loading = () => {
  return (
    <div className="is-flex is-justify-content-center">
      <span
        className="button is-loading is-medium"
        style={{ border: 'unset' }}
      ></span>
    </div>
  );
};

export default Loading;
